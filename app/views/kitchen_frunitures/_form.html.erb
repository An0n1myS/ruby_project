<div>
  <%= form_with(model: kitchen_fruniture) do |form| %>
    <% if kitchen_fruniture.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(kitchen_fruniture.errors.count, "error") %> prohibited this kitchen_fruniture from being saved:</h2>

        <ul>
          <% kitchen_fruniture.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>

    <div>
      <%= form.label :title, style: "display: block" %>
      <%= form.text_field :title %>
    </div>

    <div>
      <%= form.label :content, style: "display: block" %>
      <%= form.text_area :content %>
    </div>

    <div>
      <%= form.label :price, style: "display: block" %>
      <%= form.text_area :price %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  <% end %>
</div>